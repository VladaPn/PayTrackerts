let character = 'luigi';
console.log(character);

const inputs = document.querySelectorAll('input');

console.log(inputs);

inputs.forEach((input)=>{
    console.log(input);
})

let age = 30;
let isBlackBelt = false;

character = 'majk';

const circle = (diameter:number)=>{
    return diameter*Math.PI;
}
console.log(circle(2));

//arays
let names = ['luigi','mario','vladaOtac'];

names.push('Cica');

let mixed = ['cica',22,'tricko',12,true];
mixed.push(false);

//objekti

let ninja = {
    name:'mario',
    belt:'black',
    age:30
}


